---

IOT SECURE GATEWAY – DEVELOPMENT LOG
Phase 2 Practical Implementation Progress
Date: [Add Date]

---

1. INITIAL ENVIRONMENT SETUP

Successfully installed:

* VirtualBox
* Vagrant
* Git

Created the project directory:
iot-secure-gateway-mirai-mitigation

Initialized local Git repository and connected it to GitHub.

Repository now contains:

* Vagrantfile
* README
* Configuration scripts

---

2. VIRTUAL LAB DEPLOYMENT

Deployed a three-VM architecture using:

```
vagrant up
```

Architecture includes:

1. Firewall / Gateway VM

   * Ubuntu Server
   * nftables firewall
   * NAT configuration
   * IP forwarding enabled

2. IoT Device VM

   * Connected to private IoT LAN (10.20.0.0/24)
   * Default route set to firewall (10.20.0.1)

3. Attacker VM (External)

   * Connected to simulated WAN network (192.168.56.0/24)
   * Used for Mirai-style scanning tests

All machines booted successfully.

---

3. NETWORK VALIDATION TESTS

Verified IP forwarding:

```
cat /proc/sys/net/ipv4/ip_forward
```

Result:
1

Confirmed behavior:

* IoT VM can access outbound services (HTTPS allowed).
* Attacker VM cannot directly reach IoT VM.
* Mirai-style inbound scanning is blocked.

Example test from attacker:

```
nmap -p 22,23,2323 10.20.0.10
```

Result:
Ports filtered / unreachable.

Conclusion:
Network segmentation and routing behavior are functioning correctly.

---

4. OBSERVED ISSUE – NFTABLES RULE VISIBILITY

When executing:

```
sudo nft list ruleset
```

No rules were displayed, even though traffic filtering behavior was observed.

Possible causes identified:

1. nftables service not properly loaded.
2. Provisioning script may not have applied rules correctly.
3. Interface names may differ from expected values (eth1 vs enp0s8).
4. Traffic failure may be due to routing instead of firewall enforcement.

Diagnostics to perform next:

```
ip a
sudo systemctl status nftables
sudo nft list ruleset
cat /proc/sys/net/ipv4/ip_forward
```

---

5. VM LIFECYCLE BEHAVIOR UNDERSTANDING

Tested Vagrant commands:

vagrant halt

* Shuts down VMs
* Does NOT reinstall
* Preserves state

vagrant up

* Restarts existing machines
* Does NOT re-run provisioning

vagrant destroy

* Deletes VMs completely
* Removes all state
* Next vagrant up rebuilds from scratch

Conclusion:
For reproducibility and clean testing, use:

```
vagrant destroy -f
vagrant up
```

---

6. CURRENT STATUS

Completed:
✔ Virtual lab environment created
✔ Three-VM architecture operational
✔ Network segmentation working
✔ External attacker simulation functional
✔ IoT device not directly reachable from outside

Pending:
⚠ Verify nftables rule loading
⚠ Confirm interface naming consistency
⚠ Enable logging for blocked traffic

---

7. NEXT STEPS

* Validate nftables persistence
* Adjust interface definitions if necessary
* Add firewall logging for evidence
* Define formal Phase 3 test cases
* Capture screenshots and logs for report

---

END OF CURRENT SESSION

---
